void main(List<String> arguments) {
  print('Another day to look into dart sets');

  // to create a set we use {}
  Set mySet = {};
  mySet.add(1);

  // check if the set is empty
  var setIsEmpty = mySet.isEmpty ? mySet.isEmpty : mySet;
  print(setIsEmpty);

  // just like lists, sets has methods too used to manupulate data or items in it. Don't hesistate to check out.
}
